{
  "name": "NIS — Nursing Intelligence System",
  "install": "python3 -m pip install -e '.[dev]'",
  "terminals": [
    {
      "name": "nis-web",
      "command": "python3 -m nis_engine.cli serve --port 8081",
      "description": "Validates tools, generates static pages, and serves apps/web at http://localhost:8081."
    }
  ],
  "ports": [
    {
      "name": "nis web",
      "port": 8081
    }
  ]
}
