# Contribuir

1. Objetos novos: apenas `data/tools/{slug}.json`.
2. Depois de editar JSON: `python3 -m engine.cli build`.
3. Não editar `render/**` na mão.
4. Não marcar `published` sem evidência e aprovador.
5. Não introduzir CDN.
6. Testes em `tests/`.
