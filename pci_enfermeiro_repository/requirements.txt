# Catalog phase needs only the Python standard library (urllib + html.parser).
# The following are required only for the download phase (real browser + PDFs):
playwright==1.48.0
# Optional convenience for ad-hoc parsing / debugging:
beautifulsoup4==4.12.3
lxml==5.3.0
